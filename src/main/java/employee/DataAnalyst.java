package employee;

import employee.idCard.IDCard;

public class DataAnalyst extends Employee{
    public DataAnalyst(int id, String name, IDCard idCard, int pin, int superPin) {
        super(id, name, idCard, pin, superPin);
    }
}
